function format(index) {
  var letter = ''
  switch (index) {
    case 0:
      letter = 'A'
      break;
    case 1:
      letter = 'B'
      break;
    case 2:
      letter = 'C'
      break;
          case 2:
      letter = 'C'
      break;
    default:
      letter = 'D'
      break;
  }
  return letter
}
module.exports = {
  format: format
}